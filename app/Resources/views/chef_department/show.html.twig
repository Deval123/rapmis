{% extends 'base.html.twig' %}

{% block body %}
    <h1>Chef_department</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chef_department.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ chef_department.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('chef_department_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('chef_department_edit', { 'id': chef_department.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
