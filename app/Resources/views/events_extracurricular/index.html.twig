{% extends 'base.html.twig' %}

{% block body %}
<div class="page-content-wrapper">
    <div class="page-content">
        <div class="page-bar">
            <div class="page-title-breadcrumb">
                <div class=" pull-left">
                    <div class="page-title">Dashboard</div>
                </div>
                <ol class="breadcrumb page-breadcrumb pull-right">
                    <li><i class="fa fa-home"></i>&nbsp;<a class="parent-item" href="{{ path('homepage') }}">Home</a>&nbsp;<i class="fa fa-angle-right"></i>
                    </li>
                    <li class="active">Dashboard</li>
                </ol>
            </div>
        </div>
    <h1>Events_extracurriculars list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Role</th>
                <th>Prime</th>
                <th>Description</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for events_extracurricular in events_extracurriculars %}
            <tr>
                <td><a href="{{ path('events_extracurricular_show', { 'id': events_extracurricular.id }) }}">{{ events_extracurricular.id }}</a></td>
                <td>{{ events_extracurricular.name }}</td>
                <td>{{ events_extracurricular.role }}</td>
                <td>{{ events_extracurricular.prime }}</td>
                <td>{{ events_extracurricular.description }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('events_extracurricular_show', { 'id': events_extracurricular.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('events_extracurricular_edit', { 'id': events_extracurricular.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('events_extracurricular_new') }}">Create a new events_extracurricular</a>
        </li>
    </ul>
    </div>
</div>
{% endblock %}
