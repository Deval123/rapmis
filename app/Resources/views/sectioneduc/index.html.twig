{% extends 'base.html.twig' %}

{% block body %}
<div class="page-content-wrapper">
    <div class="page-content">
        <div class="page-bar">
            <div class="page-title-breadcrumb">
                <div class=" pull-left">
                    <div class="page-title">Dashboard</div>
                </div>
                <ol class="breadcrumb page-breadcrumb pull-right">
                    <li><i class="fa fa-home"></i>&nbsp;<a class="parent-item" href="{{ path('homepage') }}">Home</a>&nbsp;<i class="fa fa-angle-right"></i>
                    </li>
                    <li class="active">Dashboard</li>
                </ol>
            </div>
        </div>
    <h1>Sectioneducs list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for sectionEduc in sectionEducs %}
            <tr>
                <td><a href="{{ path('sectioneduc_show', { 'id': sectionEduc.id }) }}">{{ sectionEduc.id }}</a></td>
                <td>{{ sectionEduc.name }}</td>
                <td>{{ sectionEduc.description }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('sectioneduc_show', { 'id': sectionEduc.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('sectioneduc_edit', { 'id': sectionEduc.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('sectioneduc_new') }}">Create a new sectionEduc</a>
        </li>
    </ul>
    </div>
</div>
{% endblock %}
